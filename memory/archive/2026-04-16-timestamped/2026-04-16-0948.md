# Session: 2026-04-16 09:48:39 UTC
---
[[../2026-04-16|Apr 16]]

- **Session Key**: agent:main:main
- **Session ID**: 31e24658-0228-4881-966a-76aa762ec330
- **Source**: webchat

## Conversation Summary

assistant: ⚠️ Context limit exceeded. I've reset our conversation to start fresh - please try again.

To prevent this, increase your compaction buffer by setting `agents.defaults.compaction.reserveTokensFloor` to 20000 or higher in your config.
user: Sender (untrusted metadata):
```json
{
  "label": "openclaw-control-ui",
  "id": "openclaw-control-ui"
}
```

[Thu 2026-04-16 09:47 UTC] Text limit reached you know what to do
assistant: It looks like we’ve hit the context‑size ceiling. To avoid this in the future, bump the compaction‑buffer setting in the OpenClaw config:

```bash
openclaw gateway config.patch \
  --path agents.defaults.compaction.reserveTokensFloor \
  --value 20000
```

(If you prefer a direct JSON‑patch, use `gateway config.patch` with a JSON payload that sets the `agents.defaults.compaction.reserveTokensFloor` field to `20000`.)

After the change, restart the gateway so the new limit takes effect:

```bash
openclaw gateway restart
```

Once the gateway is back up, the system will keep more tokens in reserve before it starts compacting, preventing “text limit reached” errors. If you still see the warning, consider raising the value a bit higher (e.g., 30000). Feel free to let me know if you’d like me to run the patch for you.
---
[[INDEX.md|← Back]]
-e 
---
[[../2026-04-16|Apr 16]]
